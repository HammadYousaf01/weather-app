import Container from "@mui/material/Container";
import { SearchPage } from "./pages";

const App: React.FC = () => {
  return (
    <Container>
      <SearchPage />
    </Container>
  );
};

export default App;
